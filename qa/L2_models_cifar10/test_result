#!/bin/bash

read line
if [[ $line =~ ^[0-9]+"-"[0-9]+"-"[0-9]+" "[0-9]+":"[0-9]+":"[0-9]+"."[0-9]*": precision @ 1 = 0.8"[0-9]*$ ]]
then
	echo "Pass: $line"
else
	echo "Fail: $line"
	exit 1
fi

exit 0
